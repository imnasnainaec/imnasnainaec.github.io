/*
function addExample(graph6,n,hog) {
    known = new Array(props.length + 1);
    known = known.map(function() {return 0;});
    var examp = [graph6, n, known, "http://hog.grinvin.org/ViewGraphInfo.action?id=" + hog];
    return examp;
};

function randExampleCheck() {
    var i = Math.floor((Math.random() * (examples.length - 1)) + 1);
    var examp = examples[i];
    var n = 0, yes = 0;
    while ( (n<props.length) && (yes==0) ) {
        n++;
        yes = examp[2][n];
    };
    var check = prompt("Is graph", "0");
    if (person != null) {
    };
    
};
*/
